package ncl.pm.model.bc.vo;

import java.sql.CallableStatement;
import java.sql.ResultSet;

import java.sql.SQLException;

import java.sql.Statement;

import oracle.jbo.Row;
import oracle.jbo.VariableValueManager;
import oracle.jbo.ViewObject;
import oracle.jbo.server.RowQualifier;
import oracle.jbo.server.ViewObjectImpl;
import oracle.jbo.server.ViewRowImpl;
import oracle.jbo.server.ViewRowSetImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Fri Feb 12 21:35:00 YEKT 2016
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class XxpmItemMasterDyViewImpl extends ViewObjectImpl {
    /**
     * This is the default constructor (do not remove).
     */
    public XxpmItemMasterDyViewImpl() {
    }

    /**
     * executeQueryForCollection - overridden for custom java data source support.
     */
    protected void executeQueryForCollection(Object qc, Object[] params,
                                             int noUserParams) {
        super.executeQueryForCollection(qc, params, noUserParams);
    }

    /**
     * hasNextForCollection - overridden for custom java data source support.
     */
    protected boolean hasNextForCollection(Object qc) {
        boolean bRet = super.hasNextForCollection(qc);
        return bRet;
    }

    /**
     * createRowFromResultSet - overridden for custom java data source support.
     */
    protected ViewRowImpl createRowFromResultSet(Object qc,
                                                 ResultSet resultSet) {
        ViewRowImpl value = super.createRowFromResultSet(qc, resultSet);
        if (value != null) {
            oracle.jbo.domain.Number bId =
                (oracle.jbo.domain.Number)value.getAttribute("BaseImId");
            ViewObjectImpl wvItmDscVo = (ViewObjectImpl)this.getApplicationModule().findViewObject("WeavingItemDescLov");
            RowQualifier wvItmDscRQ = new RowQualifier("Id=" + bId);
            Row[] wvItmDscRows = wvItmDscVo.getFilteredRows(wvItmDscRQ);
            Row wvItmDscRow = null;
            for(int i = 0; i < wvItmDscRows.length; i++) {
                wvItmDscRow = wvItmDscRows[i];
                value.setAttribute("WvDescription",
                                   wvItmDscRow.getAttribute("Description"));
            }
            ////////////////
            String s4 = (String)value.getAttribute("Segment4");
            ViewObjectImpl itmColorVo = (ViewObjectImpl)this.getApplicationModule().findViewObject("ItemColorsLov");
            RowQualifier itmColorRQ = new RowQualifier("Value= '" + s4 + "'");
            Row[] itmColorRows = itmColorVo.getFilteredRows(itmColorRQ);
            Row itmColorRow = null;
            for(int i = 0; i < itmColorRows.length; i++) {
                itmColorRow = itmColorRows[i];
                value.setAttribute("DyColor",
                                   itmColorRow.getAttribute("Display"));
            }
            ////////////////
            String s5 = (String)value.getAttribute("Segment5");
            ViewObjectImpl itmSizeVo = (ViewObjectImpl)this.getApplicationModule().findViewObject("ItemSizeLov");
            RowQualifier itmSizeRQ = new RowQualifier("Value= '" + s5 + "'");
            Row[] itmSizeRows = itmSizeVo.getFilteredRows(itmSizeRQ);
            Row itmSizeRow = null;
            for(int i = 0; i < itmSizeRows.length; i++) {
                itmSizeRow = itmSizeRows[i];
                value.setAttribute("DyFinishWidthDesc",
                                   itmSizeRow.getAttribute("Display"));
            }
        }
        return value;
    }

    /**
     * getQueryHitCount - overridden for custom java data source support.
     */
    public long getQueryHitCount(ViewRowSetImpl viewRowSet) {
        long value = super.getQueryHitCount(viewRowSet);
        return value;
    }

    public void insertRow(Row row) {
        Row lastRow = this.last();
        if (lastRow != null) {
            int indx = this.getRangeIndexOf(lastRow) + 1;
            this.insertRowAtRangeIndex(indx, row);
            this.setCurrentRow(row);
        } else
            super.insertRow(row);
    }

    /**
     * Returns the variable value for BindDescDy.
     * @return variable value for BindDescDy
     */
    public String getBindDescDy() {
        return (String)ensureVariableManager().getVariableValue("BindDescDy");
    }

    /**
     * Sets <code>value</code> for variable BindDescDy.
     * @param value value to bind as BindDescDy
     */
    public void setBindDescDy(String value) {
        ensureVariableManager().setVariableValue("BindDescDy", value);
    }
}
