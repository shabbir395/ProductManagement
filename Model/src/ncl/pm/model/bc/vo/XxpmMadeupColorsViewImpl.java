package ncl.pm.model.bc.vo;

import java.sql.ResultSet;

import ncl.pm.model.bc.view.xxpmJC.ModelActions;

import oracle.jbo.Row;
import oracle.jbo.ViewObject;
import oracle.jbo.server.ViewObjectImpl;
import oracle.jbo.server.ViewRowImpl;
import oracle.jbo.server.ViewRowSetImpl;
// ---------------------------------------------------------------------
// ---    File generated by Oracle ADF Business Components Design Time.
// ---    Mon Jul 04 16:49:36 PKT 2016
// ---    Custom code may be added to this class.
// ---    Warning: Do not modify method signatures of generated methods.
// ---------------------------------------------------------------------
public class XxpmMadeupColorsViewImpl extends ViewObjectImpl {
    /**
     * This is the default constructor (do not remove).
     */
    public XxpmMadeupColorsViewImpl() {
    }

    /**
     * executeQueryForCollection - overridden for custom java data source support.
     */
    protected void executeQueryForCollection(Object qc, Object[] params,
                                             int noUserParams) {
        super.executeQueryForCollection(qc, params, noUserParams);
    }

    /**
     * hasNextForCollection - overridden for custom java data source support.
     */
    protected boolean hasNextForCollection(Object qc) {
        boolean bRet = super.hasNextForCollection(qc);
        return bRet;
    }

    /**
     * createRowFromResultSet - overridden for custom java data source support.
     */
    protected ViewRowImpl createRowFromResultSet(Object qc,
                                                 ResultSet resultSet) {
        ViewRowImpl value = super.createRowFromResultSet(qc, resultSet);
        if (value != null) {
            String clr = "";
            clr =
ma.getColorDesc(getDBTransaction(), (String)value.getAttribute("ColorCode"));
            value.setAttribute("ColorName", clr);
            String clr2 = "";
            clr2 =
ma.getColorDesc(getDBTransaction(), (String)value.getAttribute("ColorCode2"));
            value.setAttribute("ColorName2", clr2);
            String clr3 = "";
            clr3 =
ma.getColorDesc(getDBTransaction(), (String)value.getAttribute("ColorCode3"));
            value.setAttribute("ColorName3", clr3);
            String clr4 = "";
            clr4 =
ma.getColorDesc(getDBTransaction(), (String)value.getAttribute("ColorCode4"));
            value.setAttribute("ColorName4", clr4);
            String clr5 = "";
            clr5 =
ma.getColorDesc(getDBTransaction(), (String)value.getAttribute("ColorCode5"));
            value.setAttribute("ColorName5", clr5);
            String clr6 = "";
            clr6 =
ma.getColorDesc(getDBTransaction(), (String)value.getAttribute("ColorCode6"));
            value.setAttribute("ColorName6", clr6);
            String clr7 = "";
            clr7 =
ma.getColorDesc(getDBTransaction(), (String)value.getAttribute("ColorCode7"));
            value.setAttribute("ColorName7", clr7);
            String clr8 = "";
            clr8 =
ma.getColorDesc(getDBTransaction(), (String)value.getAttribute("ColorCode8"));
            value.setAttribute("ColorName8", clr8);
        }
        return value;
    }

    /**
     * getQueryHitCount - overridden for custom java data source support.
     */
    public long getQueryHitCount(ViewRowSetImpl viewRowSet) {
        long value = super.getQueryHitCount(viewRowSet);
        return value;
    }

    ModelActions ma = new ModelActions();

    public void insertRow(Row row) {
        Row lastRow = this.last();
        if (lastRow != null) {
            int indx = this.getRangeIndexOf(lastRow) + 1;
            this.insertRowAtRangeIndex(indx, row);
            this.setCurrentRow(row);
        } else
            super.insertRow(row);
    }
}
